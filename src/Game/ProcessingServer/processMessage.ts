import { IProcessProps } from '../../Network/Packets/IMsgBase';
import { logger } from '../../Services/LoggingService';

export async function processGameProcessingMessage(props: IProcessProps) {
  logger.error('Not implemented');
}
